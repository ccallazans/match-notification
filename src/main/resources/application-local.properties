# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/match-notification
spring.datasource.username=postgres
spring.datasource.password=postgres

# AWS Configuration
cloud.aws.region=us-east-1
cloud.aws.sqs.notification-queue=https://sqs.us-east-1.amazonaws.com/080324327804/sqs-match-notification

# JWT
api.security.token.secret=secret

# JPA
spring.jpa.hibernate.ddl-auto=validate
#spring.jpa.show-sql=true
#spring.jpa.properties.hibernate.format_sql=true

# OpenAPI
springdoc.api-docs.path=/api-docs